# Base URL of the Tydee API, used by the browser (Bearer-token calls)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Same API base URL, used server-side by the auth route handlers (cookie handling)
API_URL=http://localhost:5000
